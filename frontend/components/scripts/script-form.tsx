import NicheForm from "@/components/scripts/NicheForm";

export default function ScriptForm() {
  return (
    <NicheForm
      mode="authenticated"
      title="Script Generator"
      description="Turn a topic into a polished creator script and save it to your workspace."
    />
  );
}
